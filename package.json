{
  "name": "linecheck",
  "version": "1.1.1",
  "private": true,
  "scripts": {
    "prepare": "git config core.hooksPath .githooks || true",
    "changeset": "changeset",
    "version": "changeset version && node scripts/sync-version.mjs",
    "release": "bash scripts/publish.sh && bash scripts/publish-npm.sh"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "block-no-verify": "^1.3.0"
  }
}
