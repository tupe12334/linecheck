---
"linecheck": minor
---

Add a `--skip-whitespace` flag that excludes blank and whitespace-only lines from a file's counted line total. Off by default — existing counts and thresholds are unchanged unless the flag is passed.
