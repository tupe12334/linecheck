---
"linecheck": minor
---

Add a composite `action.yml` so linecheck can be consumed via `uses: tupe12334/linecheck@main` in GitHub Actions workflows, improving discoverability on the GitHub Marketplace (#21).
